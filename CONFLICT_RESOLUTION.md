# Merge Conflict Resolution

This branch resolves the merge conflicts in PR #2
(copilot/add-squid-registry-feature → main).

## Conflict Details

**File:** `README.md`

**Resolution:** The PR branch had the default Vite template README, while
`main` was updated with the project-specific `zoote.goose` README. The conflict
was resolved by keeping `main`'s README, which contains the detailed project
whitepaper, SQUID registry documentation, and author information.

The resolved merge commit has been applied to the
`copilot/add-squid-registry-feature` branch.
